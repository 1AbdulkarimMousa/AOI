export const projectsTemplate = String.raw`
<section x-show="view==='projects'" class="project-workspace" aria-labelledby="project-heading">
  <div x-show="projectLoading" class="project-state project-skeleton" role="status"><strong>Loading project operating context…</strong><span></span><span></span></div>
  <div x-show="projectError" class="admin-notice error project-state" role="alert"><span x-text="projectError"></span><button class="button button-secondary" @click="projectSwitcherValue ? refreshProjectSnapshot() : refreshProjectContext()">Retry project</button></div>
  <div x-show="projectReconciliationWarning" class="admin-notice warning project-state" role="status" aria-live="polite"><span x-text="projectReconciliationWarning"></span><button class="button button-secondary" @click="reconcileSelectedProject()">Retry reconciliation</button></div>
  <div x-show="projectStale" class="admin-notice warning project-state" role="alert"><span>This project changed in another session. Your draft is preserved.</span><button class="button button-secondary" @click="reloadStaleProjectRecord()">Load current record</button></div>
  <template x-if="projectSnapshot">
    <div>
      <header class="project-header">
        <div><span class="eyebrow" x-text="activeProject?.code+' · Project operating core'"></span><h1 id="project-heading" x-text="activeProject?.name"></h1><p x-text="activeProject?.objective||'No objective has been recorded.'"></p></div>
        <div class="project-health"><small>Health</small><strong :class="'project-tone-'+(activeProject?.health||'on_track')" x-text="activeProject?.healthLabel||projectStatusLabel(activeProject?.health)"></strong><span x-text="projectStatusLabel(activeProject?.status)"></span></div>
        <div x-show="activeProjectRole==='admin'" class="project-header-actions"><button class="button button-secondary" @click="startProjectCreation()">Create project</button><button class="button button-secondary" @click="startProjectConfiguration()">Configure project</button></div>
      </header>
      <article x-show="projectAdminMode" class="project-admin-pane" tabindex="-1" aria-labelledby="project-admin-heading" @keydown.escape.stop="closeProjectAdmin()">
        <div class="project-detail-header"><span class="eyebrow" x-text="projectAdminMode==='create'?'Project administration':'Project configuration'"></span><button type="button" class="icon-button" aria-label="Close project administration" @click="closeProjectAdmin()">×</button></div>
        <h2 id="project-admin-heading" x-text="projectAdminMode==='create'?'Create project':'Configure project'"></h2>
        <p x-show="preview" class="project-preview-label">Preview only: these forms demonstrate behavior without persistence.</p>
        <form class="project-form" @input="projectDirty=true" @change="projectDirty=true" @submit.prevent="saveAdminProject()">
          <fieldset class="project-form-fields" :disabled="projectSaving"><div class="project-form-grid"><label><span>Project name</span><input x-model.trim="projectAdminDraft.name" required></label><label><span>Project code</span><input x-model.trim="projectAdminDraft.code" required></label></div><label><span>Project objective</span><textarea rows="3" x-model.trim="projectAdminDraft.objective"></textarea></label><div class="project-form-grid"><label><span>Sponsor</span><input x-model.trim="projectAdminDraft.sponsorName"></label><label><span>Manager</span><select x-model="projectAdminDraft.managerId"><option value="">Unassigned</option><template x-for="member in projectSnapshot.members||[]" :key="member.userId||member.user_id"><option :value="member.userId||member.user_id" x-text="member.displayName||member.display_name"></option></template></select></label></div><div class="project-form-grid"><label><span>Planned start</span><input type="date" x-model="projectAdminDraft.plannedStart"></label><label><span>Planned finish</span><input type="date" x-model="projectAdminDraft.plannedFinish"></label></div><label><span>Health</span><select x-model="projectAdminDraft.health"><option value="on_track">On track</option><option value="at_risk">At risk</option><option value="off_track">Off track</option></select></label></fieldset>
          <div class="project-save-row"><button type="button" class="button button-secondary" @click="closeProjectAdmin()">Cancel</button><button class="button button-primary" :disabled="projectSaving">Save project</button></div>
        </form>
        <section x-show="projectAdminMode==='configure'" class="project-membership"><h3>Project membership</h3><template x-for="member in projectSnapshot.members||[]" :key="member.userId||member.user_id"><div><span><strong x-text="member.displayName||member.display_name||'Unavailable member'"></strong><small x-text="member.responsibility||'No responsibility recorded'"></small></span><button type="button" class="button button-secondary" :disabled="projectSaving" @click="saveProjectMember({...member,userId:member.userId||member.user_id,active:false})">Deactivate</button></div></template><form class="project-member-form" @submit.prevent="saveProjectMember()"><label><span>Eligible organization member</span><select x-model="projectMemberDraft.userId" required><option value="">Choose a person</option><template x-for="member in projectSnapshot.eligibleOrganizationMembers||[]" :key="member.userId"><option :value="member.userId" x-text="member.displayName+' · '+member.role"></option></template></select></label><label><span>Responsibility</span><input x-model.trim="projectMemberDraft.responsibility" required></label><button class="button button-secondary" :disabled="projectSaving">Add or reactivate member</button></form></section>
        <section x-show="projectAdminMode==='configure'" class="project-lifecycle"><h3>Project lifecycle and health</h3><label><span>Lifecycle note</span><textarea rows="2" x-model.trim="projectLifecycleNote"></textarea></label><div><button x-show="activeProject?.status==='planning'" class="button button-secondary" @click="transitionActiveProject('activate')">Activate project</button><button x-show="activeProject?.status==='active'" class="button button-secondary" @click="transitionActiveProject('hold')">Put on hold</button><button x-show="activeProject?.status==='active'" class="button button-secondary" @click="transitionActiveProject('complete')">Complete project</button><button x-show="activeProject?.status==='on_hold'" class="button button-secondary" @click="transitionActiveProject('reopen')">Reopen project</button><button x-show="activeProject?.status==='completed'" class="button button-secondary" @click="transitionActiveProject('archive')">Archive project</button></div></section>
      </article>
      <nav class="project-tabs workspace-local-tabs" aria-label="Project sections" role="tablist">
        <button id="project-tab-overview" type="button" role="tab" aria-controls="project-panel-overview" :tabindex="projectTab==='overview'?0:-1" :aria-selected="projectTab==='overview'" :class="projectTab==='overview'&&'active'" @keydown="onProjectTabKeydown($event,'overview')" @click="setProjectTab('overview')">Overview</button>
        <button id="project-tab-milestones" type="button" role="tab" aria-controls="project-panel-milestones" :tabindex="projectTab==='milestones'?0:-1" :aria-selected="projectTab==='milestones'" :class="projectTab==='milestones'&&'active'" @keydown="onProjectTabKeydown($event,'milestones')" @click="setProjectTab('milestones')">Milestones</button>
        <button id="project-tab-blockers" type="button" role="tab" aria-controls="project-panel-blockers" :tabindex="['blockers','risks'].includes(projectTab)?0:-1" :aria-selected="['blockers','risks'].includes(projectTab)" :class="['blockers','risks'].includes(projectTab)&&'active'" @keydown="onProjectTabKeydown($event,'blockers')" @click="setProjectTab('blockers')">Blockers & Risks</button>
        <button id="project-tab-decisions" type="button" role="tab" aria-controls="project-panel-decisions" :tabindex="projectTab==='decisions'?0:-1" :aria-selected="projectTab==='decisions'" :class="projectTab==='decisions'&&'active'" @keydown="onProjectTabKeydown($event,'decisions')" @click="setProjectTab('decisions')">Decisions</button>
      </nav>

      <div id="project-panel-overview" role="tabpanel" aria-labelledby="project-tab-overview" x-show="projectTab==='overview'" class="project-overview">
        <section class="project-brief"><span class="eyebrow">Operating readout</span><h2>What needs attention now</h2><dl><div><dt>Owner</dt><dd x-text="activeProject?.managerName||'Unassigned'"></dd></div><div><dt>Next milestone</dt><dd x-text="projectSnapshot?.milestones?.[0]?.title||'No milestone scheduled'"></dd></div><div><dt>Active blockers</dt><dd x-text="(projectSnapshot?.blockers||[]).filter(item=>item.status!=='resolved').length"></dd></div><div><dt>Risks for review</dt><dd x-text="(projectSnapshot?.risks||[]).filter(item=>['identified','assessing','mitigating','monitoring'].includes(item.status)).length"></dd></div><div><dt>Decisions awaiting action</dt><dd x-text="(projectSnapshot?.decisions||[]).filter(item=>['submitted','resubmitted','revision_requested'].includes(item.status)).length"></dd></div></dl></section>
        <section class="project-activity"><span class="eyebrow">Recent change</span><template x-for="event in projectSnapshot?.activity||[]" :key="event.id"><div><strong x-text="event.actorName"></strong><span x-text="event.action"></span><small x-text="formatDate(event.occurredAt)"></small></div></template><div x-show="!projectSnapshot?.activity?.length" class="empty-state compact"><strong>No recent project changes.</strong><p>Lifecycle updates and governance actions will appear here.</p></div></section>
      </div>

      <div :id="projectTab==='milestones'?'project-panel-milestones':projectTab==='decisions'?'project-panel-decisions':'project-panel-blockers'" role="tabpanel" :aria-labelledby="projectTab==='milestones'?'project-tab-milestones':projectTab==='decisions'?'project-tab-decisions':'project-tab-blockers'" x-show="projectTab!=='overview'" class="project-register-detail" :class="selectedProjectRecord&&'has-selection'">
        <section class="project-register" aria-label="Project register">
          <div class="project-register-toolbar"><div><span class="eyebrow" x-text="projectRegisterTitle"></span><h2 x-text="filteredProjectRecords.length+(filteredProjectRecords.length===1?' record':' records')"></h2></div><div><input type="search" aria-label="Filter project records" x-model.debounce.150ms="projectFilters.query" placeholder="Filter register"><select aria-label="Sort project records" x-model="projectFilters.sort"><option value="dueDate">Due date</option><option x-show="projectTab==='risks'" value="riskScore">Risk score</option></select><button x-show="activeProjectRole==='admin' || ['blockers','risks'].includes(projectTab)" class="button button-primary" @click="startProjectRecord(projectTypeForTab(projectTab))">New <span x-text="projectTypeForTab(projectTab)"></span></button></div></div>
          <div x-show="['blockers','risks'].includes(projectTab)" class="project-subtabs" role="tablist" aria-label="Blocker and risk registers"><button role="tab" :aria-selected="projectTab==='blockers'" :class="projectTab==='blockers'&&'active'" @click="setProjectTab('blockers')">Blockers</button><button role="tab" :aria-selected="projectTab==='risks'" :class="projectTab==='risks'&&'active'" @click="setProjectTab('risks')">Risks</button></div>
          <div class="project-register-head" aria-hidden="true"><span>Record</span><span>Owner</span><span>State</span><span>Due / review</span></div>
          <template x-for="record in filteredProjectRecords" :key="record.id"><button class="project-register-row" :class="selectedProjectRecord?.id===record.id&&'active'" @click="openProjectRecord(record,$event.currentTarget)"><span><strong x-text="record.title||record.statement"></strong><small x-text="record.outcome||record.nextAction||record.description"></small></span><span x-text="record.ownerName||'Unassigned'"></span><span><em class="status-badge" x-text="projectStatusLabel(record.status)"></em><small x-show="projectTab==='risks'" x-text="riskPresentation(record.score).label+' · '+(record.score??'not scored')"></small></span><span x-text="formatDate(record.dueDate||record.reviewDate||record.plannedDate)"></span></button></template>
          <div x-show="!filteredProjectRecords.length" class="empty-state"><strong>No records match this register.</strong><p>Clear filters or add the first accountable record.</p></div>
        </section>

        <article x-show="selectedProjectRecord || projectEditorMode" class="project-detail-pane" tabindex="-1" aria-labelledby="project-detail-heading" @keydown.escape.stop="requestCloseProjectRecord()">
          <div class="project-detail-header"><button class="text-button project-detail-back" @click="requestCloseProjectRecord()">← Register</button><span class="eyebrow" x-text="projectEditorMode==='new'?'New '+projectRecordType:projectStatusLabel(selectedProjectRecord?.status)"></span><button class="icon-button" aria-label="Close detail" @click="requestCloseProjectRecord()">×</button></div>
          <h2 id="project-detail-heading" x-text="projectRecordDraft.title||projectRecordDraft.statement||selectedProjectRecord?.title||selectedProjectRecord?.statement||'New project record'"></h2>
          <p class="project-detail-summary" x-text="selectedProjectRecord?.outcome||selectedProjectRecord?.description||selectedProjectRecord?.nextAction"></p>
          <form class="project-form" @input="projectDirty=true" @change="projectDirty=true" @submit.prevent="saveProjectRecord()">
            <fieldset class="project-form-fields" :disabled="projectDetailLoading || !canEditSelectedProjectRecord">
            <label x-show="['milestone','risk','decision'].includes(projectRecordType)"><span>Owner</span><select x-model="projectRecordDraft.ownerId" required><option value="">Choose an authorized member</option><template x-for="member in projectSnapshot.members||[]" :key="member.userId||member.user_id"><option :value="member.userId||member.user_id" x-text="member.displayName||member.display_name"></option></template></select></label>
            <label x-show="projectRecordType==='blocker'"><span>Resolution owner</span><select x-model="projectRecordDraft.resolutionOwnerId" required><option value="">Choose an authorized member</option><template x-for="member in projectSnapshot.members||[]" :key="member.userId||member.user_id"><option :value="member.userId||member.user_id" x-text="member.displayName||member.display_name"></option></template></select></label>
            <label x-show="projectRecordType==='decision'"><span>Decision maker</span><select x-model="projectRecordDraft.decisionMakerId"><option value="">Assigned at approval</option><template x-for="member in projectSnapshot.members||[]" :key="member.userId||member.user_id"><option :value="member.userId||member.user_id" x-text="member.displayName||member.display_name"></option></template></select></label>
            <label x-show="projectRecordType!=='risk'"><span>Title</span><input x-model.trim="projectRecordDraft.title" required></label>
            <label x-show="projectRecordType==='risk'"><span>Risk statement</span><textarea rows="3" x-model.trim="projectRecordDraft.statement" required></textarea></label>
            <label x-show="projectRecordType==='milestone'"><span>Intended outcome</span><textarea rows="3" x-model.trim="projectRecordDraft.outcome"></textarea></label>
            <div x-show="projectRecordType==='milestone'" class="project-form-grid"><label><span>Planned finish</span><input type="date" x-model="projectRecordDraft.plannedDate"></label><label><span>Progress percent</span><input type="number" min="0" max="100" x-model.number="projectRecordDraft.progress"></label></div>
            <label x-show="projectRecordType==='milestone'"><span>Acceptance criteria</span><textarea rows="3" x-model.trim="projectRecordDraft.acceptanceCriteria"></textarea></label>
            <div x-show="projectRecordType==='risk'" class="project-form-grid"><label><span>Probability, 1 to 5</span><input type="number" min="1" max="5" x-model.number="projectRecordDraft.probability"></label><label><span>Impact, 1 to 5</span><input type="number" min="1" max="5" x-model.number="projectRecordDraft.impact"></label></div>
            <label x-show="projectRecordType==='risk'"><span>Trigger</span><textarea rows="2" x-model.trim="projectRecordDraft.trigger"></textarea></label><label x-show="projectRecordType==='risk'"><span>Mitigation</span><textarea rows="3" x-model.trim="projectRecordDraft.mitigation"></textarea></label>
            <label x-show="projectRecordType==='blocker'"><span>Description</span><textarea rows="3" x-model.trim="projectRecordDraft.description"></textarea></label><div x-show="projectRecordType==='blocker'" class="project-form-grid"><label><span>Blocking party</span><input x-model.trim="projectRecordDraft.blockingParty"></label><label><span>Expected resolution</span><input type="date" x-model="projectRecordDraft.expectedResolutionDate"></label></div><label x-show="projectRecordType==='blocker'"><span>Impact</span><select x-model="projectRecordDraft.impact"><option value="low">Low</option><option value="medium">Medium</option><option value="high">High</option><option value="critical">Critical</option></select></label>
            <label x-show="projectRecordType==='decision'"><span>Decision statement</span><textarea rows="3" x-model.trim="projectRecordDraft.statement"></textarea></label><label x-show="projectRecordType==='decision'"><span>Alternatives considered</span><textarea rows="3" x-model.trim="projectRecordDraft.alternatives"></textarea></label><label x-show="projectRecordType==='decision'"><span>Rationale</span><textarea rows="3" x-model.trim="projectRecordDraft.rationale"></textarea></label><label x-show="projectRecordType==='decision'"><span>Expected impact</span><textarea rows="3" x-model.trim="projectRecordDraft.expectedImpact"></textarea></label>
            <section x-show="projectRecordType==='decision'" class="project-evidence-editor"><div><span>Decision evidence links</span><button type="button" class="button button-secondary" @click="addProjectEvidenceLink()">Add evidence link</button></div><template x-for="(link,index) in projectRecordDraft.evidenceLinks||[]" :key="index"><fieldset><label><span>Evidence record ID</span><input x-model.trim="link.evidenceId" required></label><label><span>Stance</span><select x-model="link.stance"><option value="supporting">Supporting</option><option value="contradicting">Contradictory</option></select></label><label><span>Relevance note</span><input x-model.trim="link.relevanceNote" required></label><button type="button" class="icon-button" aria-label="Remove evidence link" @click="removeProjectEvidenceLink(index)">×</button></fieldset></template><p x-show="!projectRecordDraft.evidenceLinks?.length">Approval requires at least one eligible evidence record. Supporting and contradictory links use the same fields and hierarchy.</p></section>
            <label x-show="projectRecordType!=='decision'"><span>Next action</span><textarea rows="2" x-model.trim="projectRecordDraft.nextAction"></textarea></label><label x-show="projectRecordType!=='decision'"><span>Next action due</span><input type="date" x-model="projectRecordDraft.dueDate"></label><label x-show="projectRecordType==='risk'"><span>Review date</span><input type="date" x-model="projectRecordDraft.reviewDate"></label>
            </fieldset>
            <div x-show="canEditSelectedProjectRecord" class="project-save-row"><button type="button" class="button button-secondary" @click="requestCloseProjectRecord()">Cancel edits</button><button class="button button-primary" :disabled="projectSaving || projectDetailLoading" x-text="projectSaving?'Saving…':'Save record'"></button></div>
          </form>
          <div x-show="projectRecordType==='decision' && selectedProjectRecord" class="project-evidence-balance"><section><h3>Supporting evidence</h3><template x-for="evidence in selectedProjectRecord?.evidence?.supporting||[]" :key="evidence.id"><article><strong x-text="evidence.title||evidence.relevanceNote||'Linked eligible evidence'"></strong><span x-text="evidence.provenance||evidence.evidence_id||''"></span><small x-text="evidence.limitations||evidence.relevanceNote||''"></small></article></template></section><section><h3>Contradictory evidence</h3><template x-for="evidence in selectedProjectRecord?.evidence?.contradicting||[]" :key="evidence.id"><article><strong x-text="evidence.title||evidence.relevanceNote||'Linked contradictory evidence'"></strong><span x-text="evidence.provenance||evidence.evidence_id||''"></span><small x-text="evidence.limitations||evidence.relevanceNote||''"></small></article></template></section></div>
          <section x-show="projectRecordType==='decision' && selectedProjectRecord?.snapshots?.length" class="project-approved-snapshot"><h3>Immutable approved snapshot</h3><template x-for="snapshot in selectedProjectRecord?.snapshots||[]" :key="snapshot.id"><article><header><strong x-text="'Version '+snapshot.version"></strong><span x-text="formatDate(snapshot.approvedAt||snapshot.approved_at)"></span><small>Read-only governance record</small></header><dl><div><dt>Decision statement</dt><dd x-text="snapshot.content?.statement||snapshot.statement"></dd></div><div><dt>Alternatives</dt><dd x-text="(snapshot.content?.alternatives||snapshot.alternatives||[]).join?.('; ')||snapshot.content?.alternatives||snapshot.alternatives"></dd></div><div><dt>Rationale</dt><dd x-text="snapshot.content?.rationale||snapshot.rationale"></dd></div><div><dt>Expected impact</dt><dd x-text="snapshot.content?.expectedImpact||snapshot.expectedImpact||snapshot.expected_impact"></dd></div></dl><div class="project-evidence-balance project-snapshot-evidence"><section><h4>Snapshot evidence · Supporting</h4><template x-for="evidence in snapshot.evidence?.supporting||[]" :key="evidence.sourceId||evidence.evidenceId"><div><strong x-text="evidence.title"></strong><span><b>Provenance</b> <span x-text="typeof evidence.provenance==='string'?evidence.provenance:JSON.stringify(evidence.provenance||{})"></span></span><small><b>Limitations</b> <span x-text="evidence.limitations||'None recorded'"></span></small></div></template></section><section><h4>Snapshot evidence · Contradictory</h4><template x-for="evidence in snapshot.evidence?.contradicting||[]" :key="evidence.sourceId||evidence.evidenceId"><div><strong x-text="evidence.title"></strong><span><b>Provenance</b> <span x-text="typeof evidence.provenance==='string'?evidence.provenance:JSON.stringify(evidence.provenance||{})"></span></span><small><b>Limitations</b> <span x-text="evidence.limitations||'None recorded'"></span></small></div></template></section></div></article></template></section>
          <section x-show="selectedProjectRecord?.history?.length" class="project-review-history"><h3>Review history</h3><template x-for="entry in selectedProjectRecord?.history||[]" :key="entry.id"><div><strong x-text="entry.actorName||'AOI'"></strong><span x-text="projectActionLabel(entry.action)"></span><small x-text="entry.note||formatDate(entry.occurredAt||entry.created_at)"></small></div></template></section>
          <section x-show="selectedProjectRecord && collaboration" class="collaboration collaboration--full" aria-labelledby="project-collaboration-heading">
            <header class="collaboration-header"><div><span class="eyebrow">Contextual work</span><h3 id="project-collaboration-heading">Collaboration</h3><p x-text="(collaboration?.commentCount||0)+' comments attached to this record.'"></p></div><button type="button" class="button button-secondary" :aria-pressed="collaboration?.isFollowing" :disabled="collaborationMutation" @click="toggleCollaborationFollow()" x-text="collaborationFollowText()"></button></header>
            <p x-show="preview" class="project-preview-label">Preview only: collaboration stays in this browser session.</p>
            <div class="collaboration-thread" aria-label="Contextual comments"><template x-for="comment in collaboration?.comments||[]" :key="comment.id"><article class="collaboration-comment"><header><strong x-text="comment.authorName"></strong><span x-text="comment.authorRole"></span><time x-text="formatDate(comment.createdAt)"></time><em x-show="comment.editedAt">Edited</em></header><p x-text="comment.body"></p><div x-show="comment.revisions?.length" class="collaboration-revisions"><details><summary x-text="'Revision history · '+comment.revisions.length"></summary><template x-for="revision in comment.revisions" :key="revision.revision"><div><strong x-text="'Version '+revision.revision"></strong><span x-text="revision.body"></span><small x-text="revision.changeReason||formatDate(revision.createdAt)"></small></div></template></details></div><button x-show="comment.canRevise && collaborationRevision.commentId!==comment.id" type="button" class="text-button" @click="startCommentRevision(comment)">Revise comment</button><form x-show="collaborationRevision.commentId===comment.id" class="collaboration-revision-form" @keydown.escape.stop.prevent="cancelCommentRevision()" @submit.prevent="saveCommentRevision()"><label><span>Revised comment</span><textarea rows="3" x-model.trim="collaborationRevision.body" required></textarea></label><label><span>Revision reason</span><input minlength="3" x-model.trim="collaborationRevision.reason" required></label><div><button type="button" class="button button-secondary" @click="cancelCommentRevision()">Cancel revision</button><button class="button button-primary" :disabled="collaborationMutation">Save revision</button></div></form></article></template><div x-show="!collaboration?.comments?.length" class="empty-state compact"><strong>No contextual comments yet.</strong><p>Add evidence, guidance, or a blocker that belongs with this record.</p></div></div>
            <form class="collaboration-composer" @submit.prevent="submitCollaborationComment()"><label><span>Contextual comment</span><textarea rows="3" x-model.trim="collaborationDraft.body" placeholder="Add evidence, guidance, or a blocker." required></textarea></label><fieldset class="collaboration-mentions"><legend>Mention authorized collaborators</legend><template x-for="member in collaboration?.eligibleCollaborators||[]" :key="member.userId"><label><input type="checkbox" :checked="collaborationDraft.mentionedUserIds.includes(member.userId)" @change="toggleCollaborationMention(member.userId)"><span><strong x-text="member.displayName"></strong><small x-text="member.role"></small></span></label></template></fieldset><button class="button button-secondary" :disabled="collaborationMutation">Add comment</button></form>
            <form class="collaboration-handoff" @submit.prevent="submitCollaborationHandoff()"><label><span>Handoff to</span><select x-model="collaborationDraft.handoffRecipientId" required><option value="">Choose an authorized person</option><template x-for="member in collaboration?.eligibleCollaborators||[]" :key="member.userId"><option :value="member.userId" x-text="member.displayName+' · '+member.role"></option></template></select></label><label><span>Handoff reason</span><textarea rows="3" minlength="12" x-model.trim="collaborationDraft.handoffReason" placeholder="Explain the unresolved work and expected next action." required></textarea></label><p>Handoff transfers expected follow-up, not approval authority or lifecycle ownership.</p><button class="button button-secondary" :disabled="collaborationMutation">Send reasoned handoff</button></form>
            <div x-show="collaborationNotice" class="admin-notice collaboration-notice" :class="collaborationNotice?.tone" :role="collaborationNotice?.tone==='error'?'alert':'status'" aria-live="polite"><span x-text="collaborationNotice?.text"></span><button x-show="collaborationNotice?.retry" type="button" class="button button-secondary" @click="retryCollaborationMutation()">Retry</button><button x-show="collaborationNotice?.tone==='error'" type="button" class="button button-secondary" @click="refreshCollaboration()">Refresh</button></div>
          </section>
          <div x-show="selectedProjectRecord" class="project-lifecycle"><label x-show="selectedProjectActions.some(action=>action!=='supersede')"><span>Transition note</span><textarea rows="2" x-model.trim="projectTransitionNote" placeholder="Required context for review, rejection, resolution, or acceptance."></textarea></label><label x-show="selectedProjectActions.includes('supersede')"><span>Replacement approved decision ID</span><input x-model.trim="projectSupersedeDecisionId" required placeholder="Approved decision UUID"></label><div><template x-for="action in selectedProjectActions" :key="action"><button class="button button-secondary" :disabled="projectSaving || (action==='supersede'&&!projectSupersedeDecisionId.trim())" @click="transitionProjectRecord(action)" x-text="projectActionLabel(action)"></button></template></div></div>
          <div x-show="projectNotice" class="admin-notice" :class="projectNotice?.tone" role="status" aria-live="polite" x-text="projectNotice?.text"></div>
        </article>
      </div>
      <div x-show="projectUnavailable" class="empty-state project-unavailable" role="status"><strong>Record unavailable</strong><p>The requested record is unavailable in the selected authorized project. The URL has been normalized.</p></div>
    </div>
  </template>
  <div x-show="!projectLoading&&!projectError&&!projectSnapshot" class="empty-state project-state"><strong>Choose a project to begin.</strong><p>The workspace will not guess when more than one authorized context is available.</p></div>
</section>`;

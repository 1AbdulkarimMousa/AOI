export const surveyRunnerTemplate = String.raw`
<main class="survey-runner-shell" x-data="surveyRunner" x-init="init()" x-cloak>
  <header class="survey-runner-header"><a href="./" class="survey-runner-brand" aria-label="Ambiloop Ops"><span>A</span><strong>Ambiloop Research</strong></a><button class="language-button" @click="switchLocale()"><span>◎</span><span x-text="locale==='en'?'中文':'EN'"></span></button></header>
  <div class="survey-runner-live" aria-live="polite" x-text="liveStatus"></div>
  <section x-show="loading" class="survey-runner-state"><span class="spin">◌</span><h1>Opening secure survey</h1><p>Your link and published version are being verified.</p></section>
  <section x-show="ready && error && !started" class="survey-runner-state"><span class="survey-state-mark">!</span><h1>Survey unavailable</h1><p x-text="error"></p></section>

  <template x-if="ready && definition && !started">
    <section class="survey-runner-welcome"><span class="eyebrow">AOI research questionnaire</span><h1 x-text="text(definition.title)"></h1><p x-text="text(definition.description)||'Your responses support a reviewed research decision. Please answer from your own experience.'"></p><div class="survey-runner-facts"><span><strong x-text="definition.blocks.length"></strong><small x-text="locale==='zh-CN'?'部分':'sections'"></small></span><span><strong x-text="surveyQuestions(definition).length"></strong><small x-text="locale==='zh-CN'?'问题':'questions'"></small></span><span><strong>Private</strong><small x-text="link.identityMode"></small></span></div><label class="survey-consent"><input type="checkbox" x-model="consentAccepted"><span><strong x-text="locale==='zh-CN'?'研究同意':'Research consent'"></strong><small x-text="locale==='zh-CN'?'我了解我的回答将按照本问卷所述目的进行审核和分析。':'I understand my response will be reviewed and analyzed for the purpose described in this survey.'"></small></span></label><button class="button button-primary survey-start-button" :disabled="!consentAccepted||loading" @click="begin()" x-text="copy('start')"></button><small>You may close this page and resume on this device before submission.</small></section>
  </template>

  <template x-if="started && !completed">
    <div class="survey-runner-content">
      <div class="survey-runner-progress"><span><strong x-text="reviewing?(locale==='zh-CN'?'检查答案':'Review your answers'):text(currentSection()?.title)"></strong><small x-text="reviewing?'':(sectionIndex+1)+' / '+sections().length"></small></span><div class="progress-track"><span class="progress-fill progress-orange" :style="'width:'+progress()+'%'"></span></div></div>
      <section x-show="!reviewing" class="survey-runner-form"><div class="survey-runner-section-heading"><span class="eyebrow" x-text="(locale==='zh-CN'?'部分 ':'Section ')+(sectionIndex+1)"></span><h1 x-text="text(currentSection()?.title)"></h1><p x-text="text(currentSection()?.description)"></p></div><template x-for="question in currentQuestions()" :key="question.id"><fieldset class="survey-runner-question" :class="errors[question.id]&&'has-error'"><legend><span x-text="text(question.title)"></span><em x-show="question.required">*</em></legend><p x-show="text(question.description)" x-text="text(question.description)"></p>
        <template x-if="['short_text','email','phone','url','date','time','number','signature'].includes(question.type)"><input :type="({email:'email',url:'url',date:'date',time:'time',number:'number'}[question.type]||'text')" :value="answers[question.id]??''" @input="setAnswer(question.id,question.type==='number'?($event.target.value===''?'':Number($event.target.value)):$event.target.value)" :aria-invalid="Boolean(errors[question.id])" :aria-describedby="errors[question.id]?'error-'+question.id:null" :placeholder="question.type==='signature'?'Type your full name':''"></template>
        <template x-if="question.type==='calculated'"><output class="survey-calculated-output" x-text="answers[question.id]??'—'"></output></template>
        <template x-if="question.type==='long_text'"><textarea rows="5" :value="answers[question.id]??''" @input="setAnswer(question.id,$event.target.value)" :aria-invalid="Boolean(errors[question.id])" :aria-describedby="errors[question.id]?'error-'+question.id:null"></textarea></template>
        <template x-if="['single_choice','yes_no'].includes(question.type)"><div class="survey-runner-options"><template x-for="option in questionOptions(question)" :key="option.id"><label><input type="radio" :name="question.id" :value="option.id" :checked="answers[question.id]===option.id" @change="setAnswer(question.id,option.id)"><span x-text="text(option.label)"></span></label></template></div></template>
        <template x-if="question.type==='multiple_choice'"><div class="survey-runner-options"><template x-for="option in questionOptions(question)" :key="option.id"><label><input type="checkbox" :checked="(answers[question.id]||[]).includes(option.id)" @change="toggleAnswer(question.id,option.id)"><span x-text="text(option.label)"></span></label></template></div></template>
        <template x-if="question.type==='ranking'"><ol class="survey-ranking"><template x-for="(optionId,index) in rankingValues(question)" :key="optionId"><li><span x-text="index+1"></span><strong x-text="text(question.options.find(option=>option.id===optionId)?.label)"></strong><button type="button" @click="moveRanking(question,optionId,-1)" :disabled="index===0" aria-label="Move up">↑</button><button type="button" @click="moveRanking(question,optionId,1)" :disabled="index===rankingValues(question).length-1" aria-label="Move down">↓</button></li></template></ol></template>
        <template x-if="question.type==='dropdown'"><select :value="answers[question.id]??''" @change="setAnswer(question.id,$event.target.value)"><option value="">Choose an option</option><template x-for="option in questionOptions(question)" :key="option.id"><option :value="option.id" x-text="text(option.label)"></option></template></select></template>
        <template x-if="['rating','nps','likert'].includes(question.type)"><div class="survey-runner-rating"><template x-for="value in Array.from({length:(question.validation?.max??5)-(question.validation?.min??1)+1},(_,index)=>index+(question.validation?.min??1))" :key="value"><label><input type="radio" :name="question.id" :value="value" :checked="Number(answers[question.id])===value" @change="setAnswer(question.id,value)"><span x-text="value"></span></label></template></div></template>
        <template x-if="question.type==='consent'"><label class="survey-consent compact"><input type="checkbox" :checked="answers[question.id]===true" @change="setAnswer(question.id,$event.target.checked)"><span x-text="text(question.description)||text(question.title)"></span></label></template>
        <template x-if="question.type==='upload'"><label class="survey-upload-control"><input type="file" accept=".pdf,.jpg,.jpeg,.png,.txt,.csv" @change="uploadFile(question,$event)"><span><strong x-text="answers[question.id]?.name||'Choose a private file'"></strong><small>PDF, JPG, PNG, TXT or CSV · 15 MB maximum</small></span></label></template>
        <template x-if="['matrix_single','matrix_multiple'].includes(question.type)"><div class="survey-runner-matrix"><table><thead><tr><th></th><template x-for="column in question.columns||[]" :key="column.id"><th x-text="text(column.label)"></th></template></tr></thead><tbody><template x-for="row in question.rows||[]" :key="row.id"><tr><th x-text="text(row.label)"></th><template x-for="column in question.columns||[]" :key="column.id"><td><input :type="question.type==='matrix_single'?'radio':'checkbox'" :name="question.id+'-'+row.id" :checked="question.type==='matrix_single'?answers[question.id]?.[row.id]===column.id:(answers[question.id]?.[row.id]||[]).includes(column.id)" @change="setMatrixAnswer(question,row.id,column.id,$event.target.checked)" :aria-label="text(row.label)+' '+text(column.label)"></td></template></tr></template></tbody></table></div></template>
        <span class="survey-question-error" :id="'error-'+question.id" x-show="errors[question.id]" x-text="errors[question.id]" role="alert"></span></fieldset></template><div x-show="!currentQuestions().length" class="empty-state"><strong>No questions on this path.</strong><p>Continue to the next section.</p></div></section>

      <section x-show="reviewing" class="survey-runner-review"><span class="eyebrow" x-text="locale==='zh-CN'?'提交前':'Before you submit'"></span><h1>Review your answers</h1><p x-text="locale==='zh-CN'?'检查您的回答。提交后，除非研究团队要求修改，否则无法更改。':'Check your response. After submission it cannot be changed unless the research team requests a revision.'"></p><template x-for="question in allQuestions()" :key="question.id"><button @click="editQuestion(question)"><span><strong x-text="text(question.title)"></strong><small x-text="answerLabel(question,answers[question.id])"></small></span><em>Edit</em></button></template></section>
      <div x-show="error" class="admin-notice error" role="alert" x-text="error"></div><footer class="survey-runner-actions"><button class="button button-secondary" :disabled="sectionIndex===0&&!reviewing" @click="previousSection()" x-text="copy('back')"></button><button x-show="!reviewing" class="button button-primary" @click="nextSection()" x-text="sectionIndex===sections().length-1?copy('review'):copy('next')"></button><button x-show="reviewing" class="button button-primary" :disabled="submitting" @click="submitResponse()" x-text="submitting?'Submitting…':copy('submit')"></button></footer>
    </div>
  </template>

  <template x-if="completed"><section class="survey-runner-complete"><span class="survey-complete-mark">✓</span><span class="eyebrow">Response received</span><h1 x-text="text(definition.completion?.message)||'Thank you for your response.'"></h1><p>Your response is now awaiting research review. Only approved findings enter authoritative AOI analysis.</p><small>You may safely close this page.</small></section></template>
</main>`;

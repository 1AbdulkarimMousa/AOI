export const inboxTemplate = String.raw`
<section class="inbox-workspace" aria-labelledby="inbox-heading">
  <div class="inbox-intro"><div><span class="eyebrow" x-text="inboxRoleText.eyebrow"></span><h2 id="inbox-heading" x-text="inboxRoleText.heading"></h2><p x-text="inboxRoleText.body"></p></div><button class="button button-secondary" :disabled="inboxLoading" @click="refreshInbox()" x-text="inboxLoading?'Refreshing…':'Refresh work inbox'"></button></div>
  <div x-show="inboxNotice" class="admin-notice" :class="inboxNotice?.tone" role="status" aria-live="polite" x-text="inboxNotice?.text"></div>
  <nav class="inbox-buckets" aria-label="Work inbox views">
    <button :aria-pressed="inbox.bucket==='needs_action'" :class="inbox.bucket==='needs_action'&&'active'" @click="setInboxBucket('needs_action')"><span>Needs action</span><strong x-text="inboxCount('needs_action')"></strong></button>
    <button :aria-pressed="inbox.bucket==='waiting'" :class="inbox.bucket==='waiting'&&'active'" @click="setInboxBucket('waiting')"><span>Waiting on others</span><strong x-text="inboxCount('waiting')"></strong></button>
    <button :aria-pressed="inbox.bucket==='mentioned'" :class="inbox.bucket==='mentioned'&&'active'" @click="setInboxBucket('mentioned')"><span>Mentioned</span><strong x-text="inboxCount('mentioned')"></strong></button>
    <button :aria-pressed="inbox.bucket==='following'" :class="inbox.bucket==='following'&&'active'" @click="setInboxBucket('following')"><span>Following</span><strong x-text="inboxCount('following')"></strong></button>
    <button :aria-pressed="inbox.bucket==='recently_resolved'" :class="inbox.bucket==='recently_resolved'&&'active'" @click="setInboxBucket('recently_resolved')"><span>Recently resolved</span><strong x-text="inboxCount('recently_resolved')"></strong></button>
    <button :aria-pressed="inbox.bucket==='system_attention'" :class="inbox.bucket==='system_attention'&&'active'" @click="setInboxBucket('system_attention')"><span>System attention</span><strong x-text="inboxCount('system_attention')"></strong></button>
  </nav>
  <div class="inbox-layout" :class="selectedInboxItem&&'has-selection'">
    <section class="panel inbox-list" aria-label="Inbox items">
      <div class="panel-heading compact"><div><span class="eyebrow" x-text="inboxBucketName"></span><h2 x-text="inbox.items.length+(inbox.items.length===1?' item':' items')"></h2></div></div>
      <div x-show="inboxLoading" class="empty-state compact"><strong>Loading authorized work…</strong></div>
      <template x-for="item in inbox.items" :key="item.id"><button class="inbox-row" :class="[selectedInboxItem?.id===item.id&&'active',!item.readAt&&'unread']" @click="openInboxItem(item)"><span class="inbox-state" x-text="item.readAt?'Read':'Unread'"></span><span><small x-text="projectSourceLabel(item.sourceType)+' · '+item.priority"></small><strong x-text="item.summary"></strong><p x-text="item.reason"></p></span><span class="inbox-open">Open</span></button></template>
      <div x-show="!inboxLoading&&!inbox.items.length" class="empty-state"><strong>No work in this view.</strong><p>The queue is derived from authorized source records. Nothing is added to make the inbox look busy.</p></div>
    </section>
    <article x-show="selectedInboxItem" class="panel inbox-detail" aria-labelledby="inbox-item-heading" tabindex="-1">
      <button class="text-button inbox-back" @click="closeInboxItem()">← Back to inbox</button>
       <div class="panel-heading"><div><span class="eyebrow" x-text="projectSourceLabel(selectedInboxItem?.sourceType)"></span><h2 id="inbox-item-heading" x-text="selectedInboxItem?.summary"></h2><p x-text="selectedInboxItem?.reason"></p></div><span class="status-badge" x-text="selectedInboxItem?.priority"></span></div>
      <div class="inbox-actions"><button class="button button-primary" @click="openInboxSource(selectedInboxItem)">Open source record</button><button class="button button-secondary" :aria-pressed="collaboration?.isFollowing" :disabled="!collaboration||collaborationMutation" @click="toggleCollaborationFollow()" x-text="collaborationFollowText()"></button></div>
      <template x-if="collaboration"><section class="collaboration collaboration--compact" aria-labelledby="today-collaboration-heading"><h3 id="today-collaboration-heading">Recent collaboration</h3><p x-show="preview" class="project-preview-label">Preview only: collaboration stays in this browser session.</p><div class="collaboration-thread"><template x-for="comment in collaboration.comments||[]" :key="comment.id"><article class="collaboration-comment"><header><strong x-text="comment.authorName"></strong><time x-text="formatDate(comment.createdAt)"></time><em x-show="comment.editedAt">Edited</em></header><p x-text="comment.body"></p></article></template><div x-show="!collaboration.comments?.length" class="empty-state compact"><strong>No recent comments.</strong></div><button x-show="collaboration.commentCount>collaboration.comments.length" type="button" class="text-button" @click="openInboxSource(selectedInboxItem)" x-text="'View all '+collaboration.commentCount+' comments in source'"></button></div><form class="collaboration-composer" @submit.prevent="submitCollaborationComment()"><label><span>Contextual comment</span><textarea rows="3" x-model.trim="collaborationDraft.body" required></textarea></label><fieldset class="collaboration-mentions"><legend>Mention authorized collaborators</legend><template x-for="member in collaboration.eligibleCollaborators||[]" :key="member.userId"><label><input type="checkbox" :checked="collaborationDraft.mentionedUserIds.includes(member.userId)" @change="toggleCollaborationMention(member.userId)"><span x-text="member.displayName+' · '+member.role"></span></label></template></fieldset><button class="button button-secondary" :disabled="collaborationMutation">Add comment</button></form><form class="collaboration-handoff" @submit.prevent="submitCollaborationHandoff()"><label><span>Handoff to</span><select x-model="collaborationDraft.handoffRecipientId" required><option value="">Choose an authorized person</option><template x-for="member in collaboration.eligibleCollaborators||[]" :key="member.userId"><option :value="member.userId" x-text="member.displayName+' · '+member.role"></option></template></select></label><label><span>Handoff reason</span><textarea rows="3" minlength="12" x-model.trim="collaborationDraft.handoffReason" required></textarea></label><button class="button button-secondary" :disabled="collaborationMutation">Send reasoned handoff</button></form><div x-show="collaborationNotice" class="admin-notice collaboration-notice" :class="collaborationNotice?.tone" :role="collaborationNotice?.tone==='error'?'alert':'status'" aria-live="polite"><span x-text="collaborationNotice?.text"></span><button x-show="collaborationNotice?.retry" type="button" class="button button-secondary" @click="retryCollaborationMutation()">Retry</button><button x-show="collaborationNotice?.tone==='error'" type="button" class="button button-secondary" @click="refreshCollaboration()">Refresh</button></div></section></template>
    </article>
  </div>
</section>`;
